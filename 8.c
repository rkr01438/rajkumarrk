#include <stdio.h>
int main()
{
    int i, a, s=0;
    printf("Enter upper limit: ");
    scanf("%d", &a);
    for(i=1; i<=n; i++)
    {
        s += i;
    }
    printf("Sum of first %d natural numbers = %d", a, s); 
    return 0;
} 
